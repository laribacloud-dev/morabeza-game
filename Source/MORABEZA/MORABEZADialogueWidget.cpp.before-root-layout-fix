#include "MORABEZADialogueWidget.h"

#include "Components/CanvasPanel.h"
#include "Components/CanvasPanelSlot.h"
#include "Components/Border.h"
#include "Components/VerticalBox.h"
#include "Components/VerticalBoxSlot.h"
#include "Components/TextBlock.h"
#include "Blueprint/WidgetTree.h"
#include "Engine/Engine.h"
#include "Engine/GameViewportClient.h"

UMORABEZADialogueWidget::UMORABEZADialogueWidget(
    const FObjectInitializer& ObjectInitializer
)
    : Super(ObjectInitializer)
{
}

void UMORABEZADialogueWidget::NativeConstruct()
{
    Super::NativeConstruct();

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("==================================================")
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("MORABEZA DIALOGUE WIDGET: NativeConstruct")
    );

    /*
     * ============================================================
     * ROOT CANVAS
     * ============================================================
     */

    RootCanvas =
        WidgetTree->ConstructWidget<UCanvasPanel>(
            UCanvasPanel::StaticClass(),
            TEXT("DialogueRootCanvas")
        );

    if (!RootCanvas)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: FAILED TO CREATE ROOT CANVAS")
        );

        return;
    }

    WidgetTree->RootWidget = RootCanvas;

    RootCanvas->SetVisibility(
        ESlateVisibility::Visible
    );

    /*
     * ============================================================
     * DIALOGUE PANEL
     * ============================================================
     */

    DialoguePanel =
        WidgetTree->ConstructWidget<UBorder>(
            UBorder::StaticClass(),
            TEXT("DialoguePanel")
        );

    if (!DialoguePanel)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: FAILED TO CREATE DIALOGUE PANEL")
        );

        return;
    }

    RootCanvas->AddChild(DialoguePanel);

    /*
     * ============================================================
     * PANEL LAYOUT
     * ============================================================
     *
     * Bottom-center.
     *
     * The root UserWidget will be expanded to the viewport when
     * the dialogue is displayed.
     */

    UCanvasPanelSlot* PanelSlot =
        Cast<UCanvasPanelSlot>(
            DialoguePanel->Slot
        );

    if (!PanelSlot)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: FAILED TO CREATE PANEL SLOT")
        );

        return;
    }

    PanelSlot->SetAnchors(
        FAnchors(
            0.5f,
            1.0f
        )
    );

    PanelSlot->SetAlignment(
        FVector2D(
            0.5f,
            1.0f
        )
    );

    PanelSlot->SetPosition(
        FVector2D(
            0.0f,
            -80.0f
        )
    );

    PanelSlot->SetSize(
        FVector2D(
            1100.0f,
            300.0f
        )
    );

    /*
     * ============================================================
     * PANEL APPEARANCE
     * ============================================================
     */

    DialoguePanel->SetBrushColor(
        FLinearColor(
            0.01f,
            0.01f,
            0.01f,
            0.94f
        )
    );

    DialoguePanel->SetPadding(
        FMargin(
            45.0f,
            35.0f,
            45.0f,
            35.0f
        )
    );

    /*
     * ============================================================
     * CONTENT CONTAINER
     * ============================================================
     */

    DialogueContent =
        WidgetTree->ConstructWidget<UVerticalBox>(
            UVerticalBox::StaticClass(),
            TEXT("DialogueContent")
        );

    if (!DialogueContent)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: FAILED TO CREATE CONTENT")
        );

        return;
    }

    DialoguePanel->SetContent(
        DialogueContent
    );

    /*
     * ============================================================
     * SPEAKER TEXT
     * ============================================================
     */

    SpeakerText =
        WidgetTree->ConstructWidget<UTextBlock>(
            UTextBlock::StaticClass(),
            TEXT("SpeakerText")
        );

    if (SpeakerText)
    {
        SpeakerText->SetText(
            FText::FromString(
                TEXT("TEST CONTACT")
            )
        );

        SpeakerText->SetColorAndOpacity(
            FSlateColor(
                FLinearColor(
                    1.0f,
                    0.75f,
                    0.15f,
                    1.0f
                )
            )
        );

        FSlateFontInfo SpeakerFont =
            SpeakerText->GetFont();

        SpeakerFont.Size = 32;

        SpeakerText->SetFont(
            SpeakerFont
        );

        SpeakerText->SetAutoWrapText(
            true
        );

        UVerticalBoxSlot* SpeakerSlot =
            DialogueContent->AddChildToVerticalBox(
                SpeakerText
            );

        if (SpeakerSlot)
        {
            SpeakerSlot->SetPadding(
                FMargin(
                    0.0f,
                    0.0f,
                    0.0f,
                    20.0f
                )
            );
        }
    }

    /*
     * ============================================================
     * DIALOGUE TEXT
     * ============================================================
     */

    DialogueText =
        WidgetTree->ConstructWidget<UTextBlock>(
            UTextBlock::StaticClass(),
            TEXT("DialogueText")
        );

    if (DialogueText)
    {
        DialogueText->SetText(
            FText::FromString(
                TEXT("BEM-VINDO A MORABEZA.")
            )
        );

        DialogueText->SetColorAndOpacity(
            FSlateColor(
                FLinearColor(
                    1.0f,
                    1.0f,
                    1.0f,
                    1.0f
                )
            )
        );

        FSlateFontInfo DialogueFont =
            DialogueText->GetFont();

        DialogueFont.Size = 28;

        DialogueText->SetFont(
            DialogueFont
        );

        DialogueText->SetAutoWrapText(
            true
        );

        DialogueText->SetWrappingPolicy(
            ETextWrappingPolicy::AllowPerCharacterWrapping
        );

        DialogueContent->AddChildToVerticalBox(
            DialogueText
        );
    }

    /*
     * ============================================================
     * INITIAL STATE
     * ============================================================
     */

    SetVisibility(
        ESlateVisibility::Collapsed
    );

    SetRenderOpacity(
        1.0f
    );

    DialoguePanel->SetVisibility(
        ESlateVisibility::Visible
    );

    if (DialogueContent)
    {
        DialogueContent->SetVisibility(
            ESlateVisibility::Visible
        );
    }

    if (SpeakerText)
    {
        SpeakerText->SetVisibility(
            ESlateVisibility::Visible
        );
    }

    if (DialogueText)
    {
        DialogueText->SetVisibility(
            ESlateVisibility::Visible
        );
    }

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("MORABEZA DIALOGUE WIDGET: UI constructed successfully")
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("MORABEZA DIALOGUE WIDGET: Panel size = 1100 x 300")
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("==================================================")
    );
}

void UMORABEZADialogueWidget::ShowDialogue(
    const FText& SpeakerName,
    const FText& InDialogueText
)
{
    UE_LOG(
        LogTemp,
        Warning,
        TEXT("==================================================")
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("MORABEZA DIALOGUE WIDGET: ShowDialogue CALLED")
    );

    /*
     * ============================================================
     * VALIDATION
     * ============================================================
     */

    if (!RootCanvas)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: RootCanvas is NULL")
        );

        return;
    }

    if (!DialoguePanel)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: DialoguePanel is NULL")
        );

        return;
    }

    if (!DialogueContent)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: DialogueContent is NULL")
        );

        return;
    }

    if (!SpeakerText)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: SpeakerText is NULL")
        );

        return;
    }

    if (!DialogueText)
    {
        UE_LOG(
            LogTemp,
            Error,
            TEXT("MORABEZA DIALOGUE WIDGET: DialogueText is NULL")
        );

        return;
    }

    /*
     * ============================================================
     * VIEWPORT
     * ============================================================
     */

    FVector2D ViewportSize(
        1920.0f,
        1080.0f
    );

    if (UGameViewportClient* ViewportClient =
            GetWorld()
                ? GetWorld()->GetGameViewport()
                : nullptr)
    {
        ViewportClient->GetViewportSize(
            ViewportSize
        );

        UE_LOG(
            LogTemp,
            Warning,
            TEXT(
                "MORABEZA DIALOGUE WIDGET: ViewportSize X=%f Y=%f"
            ),
            ViewportSize.X,
            ViewportSize.Y
        );
    }

    /*
     * ============================================================
     * FORCE USER WIDGET TO FILL VIEWPORT
     * ============================================================
     *
     * This is the important fix.
     *
     * The UserWidget is explicitly assigned the viewport size.
     * The CanvasPanel therefore receives predictable geometry.
     */

    SetDesiredSizeInViewport(
        ViewportSize
    );

    SetPositionInViewport(
        FVector2D(
            0.0f,
            0.0f
        ),
        false
    );

    /*
     * ============================================================
     * UPDATE TEXT
     * ============================================================
     */

    SpeakerText->SetText(
        SpeakerName
    );

    DialogueText->SetText(
        InDialogueText
    );

    /*
     * ============================================================
     * FORCE VISIBILITY
     * ============================================================
     */

    SetVisibility(
        ESlateVisibility::Visible
    );

    SetRenderOpacity(
        1.0f
    );

    RootCanvas->SetVisibility(
        ESlateVisibility::Visible
    );

    DialoguePanel->SetVisibility(
        ESlateVisibility::Visible
    );

    DialogueContent->SetVisibility(
        ESlateVisibility::Visible
    );

    SpeakerText->SetVisibility(
        ESlateVisibility::Visible
    );

    DialogueText->SetVisibility(
        ESlateVisibility::Visible
    );

    /*
     * ============================================================
     * PANEL LAYOUT
     * ============================================================
     */

    UCanvasPanelSlot* PanelSlot =
        Cast<UCanvasPanelSlot>(
            DialoguePanel->Slot
        );

    if (PanelSlot)
    {
        PanelSlot->SetAnchors(
            FAnchors(
                0.5f,
                1.0f
            )
        );

        PanelSlot->SetAlignment(
            FVector2D(
                0.5f,
                1.0f
            )
        );

        PanelSlot->SetPosition(
            FVector2D(
                0.0f,
                -80.0f
            )
        );

        PanelSlot->SetSize(
            FVector2D(
                1100.0f,
                300.0f
            )
        );
    }

    /*
     * ============================================================
     * ADD TO VIEWPORT
     * ============================================================
     */

    if (!IsInViewport())
    {
        AddToViewport(
            2000
        );
    }

    /*
     * ============================================================
     * FORCE LAYOUT UPDATE
     * ============================================================
     */

    InvalidateLayoutAndVolatility();

    /*
     * ============================================================
     * DEBUG GEOMETRY
     * ============================================================
     */

    const FVector2D LocalSize =
        GetCachedGeometry().GetLocalSize();

    const FVector2D PanelLocalSize =
        DialoguePanel->GetCachedGeometry().GetLocalSize();

    UE_LOG(
        LogTemp,
        Warning,
        TEXT(
            "MORABEZA DIALOGUE WIDGET: Widget LocalSize X=%f Y=%f"
        ),
        LocalSize.X,
        LocalSize.Y
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT(
            "MORABEZA DIALOGUE WIDGET: Panel LocalSize X=%f Y=%f"
        ),
        PanelLocalSize.X,
        PanelLocalSize.Y
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("==================================================")
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("MORABEZA DIALOGUE WIDGET: DISPLAYING DIALOGUE")
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT(
            "Speaker: %s"
        ),
        *SpeakerName.ToString()
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT(
            "Text: %s"
        ),
        *InDialogueText.ToString()
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT(
            "Visibility: VISIBLE"
        )
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT(
            "Panel: 1100 x 300"
        )
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT("==================================================")
    );
}

void UMORABEZADialogueWidget::HideDialogue()
{
    SetVisibility(
        ESlateVisibility::Collapsed
    );

    UE_LOG(
        LogTemp,
        Warning,
        TEXT(
            "MORABEZA DIALOGUE WIDGET: HIDDEN"
        )
    );
}